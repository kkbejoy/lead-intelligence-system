# Runtime dependencies. Pinned to compatible ranges for reproducibility.
groq>=0.11,<1.0          # LLM API client (OpenAI-compatible, generous free tier)
python-dotenv>=1.0,<2.0  # loads GROQ_API_KEY from a local .env file
PyYAML>=6.0,<7.0         # config.yaml parsing

# Dev / test only
pytest>=8.0,<9.0
